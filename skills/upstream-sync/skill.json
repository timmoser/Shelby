{
  "name": "upstream-sync",
  "description": "Daily check and sync with upstream NanoClaw repository",
  "version": "1.0.0",
  "trigger": "daily",
  "schedule": "0 3 * * *",
  "instructions": "Fetch upstream, analyze changes, predict conflicts, and save a report. NEVER modify the working tree — report only."
}
